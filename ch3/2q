#include <iostream>
#include <string>
#include <vector>
using namespace std;

int main (int argc, char *argv[]) {
    enum fields {WORD, HINT, NUM_FIELDS};
    const int NUM_WORDS = 5;
    const string WORDS [NUM_WORDS][NUM_FIELDS] = {
        {"wall", "Do you feel you're 
    }

        
    return 0;
}
